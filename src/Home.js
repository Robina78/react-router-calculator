import React from "react";

const Home = () => {
    return (
        <div>
            <h1>Welcome to the React Router calculator!</h1>
            <p>Please modify the URL to do som math.</p>
            <p>Valid operations are add, subtract, multiply, and divide.</p>
        </div>
    );
};

export default Home;